
!.vscode/extensions.json
# (uncomment next line if you ever want to fully un-track:)
# Editor directories and files
# Existing ones already tracked (as renames) ship once; future raws stay local.
# Local env (API keys etc.)
# Logs
# Raw working-mascot PNGs (v1.9.30 moved from public/mascots to tools/raw-mascots/).
# Temp mascot generation workspace (Pollinations / Claude Design test outputs)
# rembg pre-removal backups — keep locally for re-processing, don't ship to git
# tools/raw-mascots/
*.local
*.log
*.njsproj
*.ntvs*
*.sln
*.suo
*.sw?
.DS_Store
.env
.env.local
.idea
.secrets/
.tmp-mascots/
.vscode/*
dist
dist-ssr
lerna-debug.log*
logs
node_modules
npm-debug.log*
pnpm-debug.log*
public/mascots/*.original.png
yarn-debug.log*
yarn-error.log*

# Capacitor / iOS native build artifacts (v2.0.B.262)
# 不要 commit Pods/ — 體積大,Codemagic CI 跑 pod install 重生
ios/App/Pods/
ios/App/Podfile.lock
ios/App/App.xcworkspace/xcuserdata/
ios/App/App.xcodeproj/xcuserdata/
ios/App/App.xcodeproj/project.xcworkspace/xcuserdata/
ios/.last_build_id
ios/DerivedData/
ios/build/
*.ipa
*.dSYM.zip
*.xcuserstate

# Codemagic local cache (none expected in repo)
.codemagic/

# Claude Code harness worktrees (agent isolation) — never commit
.claude/worktrees/
