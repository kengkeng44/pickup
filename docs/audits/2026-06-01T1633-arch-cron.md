# Pickup Architecture Cron — 2026-06-01 UTC

## A. Bundle size snapshot

| Chunk | Raw | Gzip |
|-------|-----|------|
| react | 139.84 kB | **45.34 kB** |
| index (app) | 59.95 kB | **17.46 kB** |
| zod | 56.50 kB | **12.93 kB** |
| react-router | 19.90 kB | **7.51 kB** |
| CSS | 23.03 kB | **5.32 kB** |
| zustand | 2.30 kB | 1.03 kB |
| rolldown-runtime | 0.69 kB | 0.42 kB |
| **TOTAL** | **302 kB** | **~90 kB** ✅ |

- **Phaser in main bundle**: ❌ 0 — React app does not import any Phaser scene. Tree-shaken out entirely.
- **Chunk > 800KB warnings**: 0
- **INEFFECTIVE_DYNAMIC_IMPORT warnings**: 0
- **Build status**: ✅ 59 modules, all 34 tests pass

### dist/ asset breakdown
| Dir | Disk |
|-----|------|
| dist/assets/ (JS+CSS) | 284 kB |
| dist/audio/ | 24 MB (peace.mp3 7.1 MB + 245 lesson MP3s 17 MB) |
| dist/mascots/ (webp icons) | ~3 MB |
| **dist/mascots/preview/ ⚠️** | **39 MB** (28 raw PNG previews, unreferenced) |
| dist/lessons-ch*.json | ~1 MB total |
| **dist/ TOTAL** | **~68 MB** |

## B. Deployed HTML size + cache headers

Site unreachable from this execution environment (network policy: "Host not in allowlist"). LCP measurement deferred to browser-based run.

## C. Spec v2 R1-R14 drift

| R# | 項目 | 現狀 | drift? | 修法 |
|----|------|------|--------|------|
| R1 | Lesson entry — no intro overlay | LessonPage.tsx goes straight to first entry, no overlay chip | ✅ | — |
| R2 | narration — auto-reveal + Continue button | NarrationRenderer auto-advances via `onEnd` callback; **no "繼續 ↓ Continue" button shown** (spec requires `.pickup-narration-continue` green btn) | ⚠️ **DRIFT** | Add `shown` state; after `onEnd`/1s show green Continue btn; fallback timer sets `shown=true` not direct advance |
| R2 | listen-tf pre-reveal — hide sentence | Pre-reveal shows `blanks(en)` word-blank shapes (`____ ____ ____`). Spec: **NO sentence text, NO blank shapes** (word count hints answer length) | ⚠️ **DRIFT** | Replace `blanks(en)` div with large round 72×72 speaker btn + bilingual cue only |
| R3 | Chinese in pre-reveal — cue label format | ListenTfRenderer shows `🎧 點喇叭聽完聲音再選答案` (Chinese only). Spec B.161.12 prescribes bilingual format: `🔊 點喇叭再聽一次 · Tap to replay` | ⚠️ minor drift | Add ` · Tap to replay` English half to the cue label |
| R4 | Scroll history — plain format | NarrativeLine renders plain text above current chunk, no green/red bg, no ✓/✕ icons | ✅ | — |
| R5 | Completion article view | CompletePanel shows stats triptych (XP/Accuracy/Time) + confetti emoji rain. **Spec: concat all `narration` sentences → "THE STORY" amber header + paragraph + audio replay + "完成 · Done" CTA** | ⚠️ **DRIFT** | `CompletePanel` needs `lesson.questions.filter(q=>q.type==='narration').map(q=>q.sentence).join(' ')` block before stat triptych; add article header + audio replay btn |
| R6 | Audio path + BGM ducking | tts.ts speak() routes to MP3 → WebSpeech fallback; duckBgm/unduckBgm in tts.ts line 212-216; App.tsx mounts BGM on first-click | ✅ | — |
| R7 | TOEIC quality | Skipped (content QA cron scope) | — | — |
| R8 | Speech rate | `u.rate = 0.75` (tts.ts:241) | ✅ | — |
| R9 | Speaker icon size | NarrationRenderer: `SpeakerBtn` default `size=22` inside calico-anchor row. Spec: **44×44 btn + 40×40 img for narration**. ListenTf pre-reveal uses `size=48` (spec: 72×72 large round). | ⚠️ minor drift | NarrationRenderer: `<SpeakerBtn size={44}>`. ListenTf pre-reveal: `size={72}` with circular amber border style |
| R10 | Mochi/Hana hardcoded | JSON files have 1× "Mochi" + 1× "Hana" in ch1. `applyCatName`/`applyDogName` kept as passthrough noop | ✅ | — |
| R11 | 5-agent audit framework | Spec + dispatch-matrix.md + player-walkthrough.md all exist. This cron = Arch agent. | ✅ | — |
| R12 | Perf budget | JS bundle 90 kB gz << 600 kB. Phaser zero bytes in main bundle. | ✅ | — |
| R13 | Capacitor / mobile | Deferred per spec | — | — |
| R14 | Memory rules | Spec references 10 memory rules; no code violations observed in audited surfaces | ✅ | — |

**Drift count: 5 items (R2×2, R3, R5, R9)**

## D. Dead code / dep status

- **Phaser imports**: 10 files in `src/scenes/*.ts` still import Phaser (BootScene, ChapterEndScene, ChapterIntroScene, EndScene, LessonScene, MenuScene, PlayScene, StoryEndingScene, StoryModeScene, bootGame.ts). **None are imported by the React app.** Phaser is 0 bytes in the live bundle but ~1.2 MB of dead source. Risk: future dev accidentally re-imports a scene file and blows the bundle.
- **Legacy `src/main.ts`**: ✅ EXISTS as dead Phaser entry point. Active entry is `src/main.tsx`. `main.ts` is not the Vite entry, so it doesn't affect the bundle.
- **Howler dep**: ✅ Not in package.json
- **`public/mascots/preview/`**: ⚠️ **39 MB** of raw PNG preview files deployed to production CDN. No code in `src/` or `public/` references these paths. Per v1.9.30 precedent (raw PNGs moved to `tools/raw-mascots/`), these should be moved to `tools/` or `.gitignore`d from `public/`.
- **`src/scenes/LessonScene.ts`**: 1424-line parallel Phaser implementation of lesson rendering. React `renderers.tsx` (459 lines) now covers all 9 renderer types. LessonScene is unreachable dead code.

## E. Daily P0/P1 (隨航)

**P0 — Top 3 architecture risks (1-2 周內會爆)**

1. **`public/mascots/preview/` 39 MB on CDN** — 28 raw PNG previews (1-2 MB each, up to 1.7 MB per file) being deployed and served from Cloudflare Pages. No source file references them. Purely wasted CDN bandwidth on every `wrangler pages deploy`. `dist/` is 68 MB total — 57% is this dead directory. Fix: `mv public/mascots/preview tools/mascot-previews` or add to `.cfpagesignore`.

2. **R2/R5 spec drift persists from previous cron (2026-06-01-0831)** — Both the narration Continue button and the listen-tf blank shapes were flagged as P1 in the UI/UX cron 8 hours ago. Zero fixes landed (B.172/B.172.1 were dashboard-only). Spec R5 completion article is a deeper drift not previously flagged: users finishing a lesson see a generic stats screen, not the "story recap" that is Pickup's Duolingo Stories brand differentiation.

3. **`src/scenes/*.ts` (Phaser) + `src/main.ts` are orphaned dead code** — 10 scene files + bootGame.ts (~4000 lines) no longer reachable from the React app. Risk: (a) tsc compiles them on every build wasting ~400ms; (b) future contributor imports a scene file expecting it to work; (c) maintenance burden confuses the codebase. Recommend `git mv src/scenes src/legacy-scenes-phaser` + comment header, or outright delete after confirming no remaining usage.

## F. Trend (vs previous cron 2026-06-01T0831)

| Metric | 2026-06-01T0831 | 2026-06-01T1633 | Δ |
|--------|-----------------|-----------------|---|
| Bundle JS+CSS gzip | not logged | ~90 kB | — |
| Spec drift items | 5 (R2×2, R3, R5, R9 — same set) | 5 | 0 |
| Phaser in bundle | 0 | 0 | 0 |
| CDN waste (preview/) | not flagged | 39 MB | ⚠️ new flag |
| Dead scene files | not flagged | 10 files | ⚠️ new flag |

R2/R5 spec drift items flagged in both the UI/UX cron and now the arch cron → **stale drift, requires dedicated fix session**.
